CREATE TABLE [dbo].[EssenceSciage] (
    [ID]          INT          IDENTITY (1, 1) NOT NULL,
    [Description] VARCHAR (25) NULL,
    CONSTRAINT [PK_EssenceSciage] PRIMARY KEY CLUSTERED ([ID] ASC)
);

